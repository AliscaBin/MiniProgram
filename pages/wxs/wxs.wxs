//JS
var message = "Hello World"
var name = "wangbin"
var sum = function(num1, num2) {
  return num1 + num2
}

var toFixedPrice = function(num){
  return num.toFixed(2)
}

//commonjs的模块化写法
module.exports = {
  message: message,
  name: name,
  sum: sum,
  toFixed: toFixedPrice
}