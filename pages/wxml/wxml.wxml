<!--pages/wxml/wxml.wxml-->
<text>wxml介绍\n</text>
<!-- 1. wxml的格式 -->
<!-- <view></view>
 <image/>
 <input/> -->
<!-- 2. Mustache语法 -->
<view>------------------</view>
<view>你好{{name}}</view>
<view>{{name}} {{learn}}</view>
<view>{{name+' '+learn}}</view>

<view style="color:orange">{{nowTime}}</view>
<view>------------------</view>
<button size="mini" bindtap="handleSwitchColor">切换颜色</button>
<view class="box {{isActive ? 'acitve':''}}">哈哈哈</view>

<view>---------条件判断---------</view>
<button size="mini" bindtap="handleSwitchShow">显示/隐藏内容</button>
<view wx:if="{{isShow}}">嘿嘿嘿，点击上方按钮隐藏我</view>

<!-- wx:elif/wx:else -->
<button size="mini" bindtap="handleIncreScore">分数递增10</button>
<view wx:if="{{score >= 90}}">优秀</view>
<view wx:elif="{{score >= 80}}">良好</view>
<view wx:elif="{{score >= 60}}">及格</view>
<view wx:else>不及格</view>

<!-- hidden属性 
hidden将一个组件隐藏起来的时候，该组件依然是存在的（display:none）
wx:if讲一个组件隐藏起来的时候，该组件根本不存在的（压根没有创建该组件）
-->
<view wx:if="{{false}}">我是wx:if属性控制的内容</view>
<view hidden="{{true}}">我是hidden属性控制的内容</view>

<view>---------列表渲染---------</view>
<!-- 1 wx:for  item 和 index -->

<!-- 1.1 遍历数组、字符串、数字 -->
<!-- <view wx:for="{{['abc','cbe','nba']}}">{{item+' '+index}}</view>
<view wx:for="{{'wangbin'}}">{{item}} {{index}}</view>
<view wx:for="{{9}}">{{item}}</view> -->

<!-- 1.2 block标签
不是组件，不被渲染 -->
<!-- <view wx:for="{{3}}">
  <button>按钮</button>
  <view>呵呵呵</view>
  <text>我是内容</text>
</view> -->
<!-- 上面的view仅仅起到包裹内部组件的作用，这时候推荐使用block -->
<!-- <block wx:for="{{3}}" wx:key="item">
  <button>按钮</button>
  <view>hehhe</view>
  <text>我是内容</text>
</block> -->

<!-- 1.3 item index 取别名 
wx:key 的作用：-->
<block wx:for="{{movies}}" wx:key="movies" wx:for-item="movie" wx:for-index="number">
<view>{{number+'->'+movie}}</view>
</block>

<!-- 模板的使用 
模板中包裹的内容在没有使用时是不会被渲染的-->
<view>------template-------</view>
<!-- <template name="contentItem">
<button>{{btnText}}</button>
<view>{{content}}</view>
</template> -->


<!-- 导入模板幷使用 -->

<!-- <include src="/pages/wxml/template.wxml"/> -->

<!-- include可以将目标文件中除了<template/> <wxml/> 以外的整个代码导入，相当于拷贝到include位置 

include可以将wxml中的公共部分抽取到一个文件中，可以递归导入关联的wxml
-->

<import src="/pages/wxml/template.wxml"/>

<template is="contentItem" data="{{btnText:'你好按钮',content:'内容'}}"/>
